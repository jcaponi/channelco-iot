export default function decorate(block) {
  const picture = block.querySelector('picture');
  const url = block.querySelector('a');
  const title = block.querySelector('h3');
  const subtitle = block.querySelector('h4');
}